spring:
  profiles:
    active: ${libintel.profile:dev}
  application:
    name: alma-connector
  cloud:
    config:
      uri: https://localhost:11811
      name: client,mail,datasource-settings,sap,service-provider,alma,mapping-tables,primo,identifier-transfer,ub-conf
      username: ${libintel_config_username:dalek}
      password: ${libintel_config_password:3l1m1nat3}
      label: ${libintel.profile:dev}
  main:
    allow-bean-definition-overriding: true
  security:
    user:
      name: ${libintel.system.username:thedoctor}
      password: ${libintel.system.password:Tard15}
      roles: SYSTEM
  jpa:
    hibernate:
      ddl-auto: create